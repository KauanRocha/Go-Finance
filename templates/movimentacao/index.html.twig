{% extends 'base.html.twig' %}

{% block title %}Go Movimentações{% endblock %}

{% block body %}
    {{ include('components/TitleComponent.html.twig', {
            'title': 'Movimentações'
    }) }}
<div class="">
    <form method="GET">
        <div class="d-flex justify-content-start">
            <label>Buscar pelo nome</label>
        </div>
        <div class="d-flex justify-content-end">
            <input type="text" name="descricao" class="form-control" value="{{ descricao ?? ''}}"/>
            <button type="submit" class="btn btn-primary">Pesquisar</button>
        </div>
    </form>
</div>

    {{
        include('components/TableComponent.html.twig', {
            'name': 'movimentacao',
            'titles': ['Descrição da movimentação', 'Valor', 'Status', 'Categoria', 'Data'],
            'items': transactions
        })
    }}

{% endblock %}
